# Netlify SPA redirects - CRITICAL for React app
# This file MUST be in public/ folder to be copied to dist/ during build

# Root URL MUST serve index.html (React dashboard)
/    /index.html   200

# Allow wasd-viewer.html for iframe (Gallery3D page needs this)
/wasd-viewer.html    /wasd-viewer.html   200

# All other routes serve index.html for SPA routing
/*    /index.html   200

